## AUTHENTICATE
type Account @aws_cognito_user_pools {
  cognitoId: String!
  createdAt: AWSDateTime!
  updatedAt: AWSDateTime!
  deletedAt: AWSDateTime
  accountName: String!
  lastLogin: String
  isActive: Boolean!
}
